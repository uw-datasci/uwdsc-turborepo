enum role_enum {
    hacker
    volunteer
    admin
    superadmin
    judge
    default
}

enum application_status_enum {
    draft
    submitted
    offered
    accepted
    rejected
    waitlisted
}
