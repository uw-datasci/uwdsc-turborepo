enum role_enum {
    hacker
    volunteer
    admin
    superadmin
    judge
    default
    declined
}

enum application_status_enum {
    draft
    submitted
    accepted
    rejected
    waitlisted
}
